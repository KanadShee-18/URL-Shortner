import { UrlShortnerContainer } from "@/components/form/url-shortner-container";
import { Navbar } from "@/components/navigation-bar";

const page = () => {
  return (
    <div>
      <Navbar />
      <UrlShortnerContainer />
    </div>
  );
};
export default page;
